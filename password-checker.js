let user = prompt("Enter your username to LOG-IN")
let pass = prompt("Enter password to LOG-IN")
let password = "admin123"
let username = "admin"
if(pass === password || user === username){
    alert("You have Logged In")
}
else{
    alert("Invalid Username or Password!!")
}
